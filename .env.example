# Auth0 Configuration
# Auth0 tenant domain (e.g., "dev-abc123.us.auth0.com")
AUTH0_DOMAIN=custom-auth0-domain.auth0.com

# Auth0 API identifier from Auth0 dashboard
# This should match API audience setting in Auth0
AUTH0_AUDIENCE=https://custom-api-identifier.com # you can make this identical to RESOURCE_SERVER_URL

# Public URL where MCP server is accessible
# Should end with "/mcp" path (e.g., "https://customdomain.com/mcp")
RESOURCE_SERVER_URL=https://custom-server-public-url.com/mcp
